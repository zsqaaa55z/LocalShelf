# 隐私说明

以下详细说明适用于本仓库未经修改的 iOS 客户端；安卓桥接端的数据、权限、临时数据库副本和二维码风险见 [安卓隐私说明](android/app/src/main/assets/licenses/PRIVACY.txt)。自建服务、修改版、系统备份及用户主动导出的数据另有各自的处理边界。

## 网络与相机

没有开发者运营的云端、广告 SDK、统计 SDK 或自动崩溃上传。客户端向用户配置的局域网阅读服务发送配对、身份检查、书库、封面和页面读取请求。服务端可观察客户端 IP、访问时间与请求中的书本／页码标识；其日志保留方式由服务端维护者负责。

安卓自动发现使用前台 Bonjour；不扫描公网。相机用于读取配对二维码，应用代码不保存或上传相机画面。也可手动输入地址和六位配对码。

当前连接使用 HTTP，传输不加密。不要把“仅局域网”理解为绝对保密。

## 设备内数据

- 配对服务地址、服务身份和阅读凭据保存在系统 Keychain，设置为本机解锁后可访问，不启用 Keychain 同步。
- 界面偏好、书本 ID、页码和最近阅读名称保存在应用本地设置中。系统是否备份这些设置受用户的设备备份策略影响。
- 封面缓存位于应用缓存目录，使用文件保护并标记不参与备份；上限为 2 GB。正文不做整本永久磁盘下载，但阅读期间会存在内存与系统显示内容。
- 用户主动导出的进度 JSON 含书库标识、书本 ID、页码及可能的最近阅读名称，不含配对 token 或图片；它仍属于敏感阅读记录。
- 跨书库迁移进度前，会在应用私有目录保存一份本地恢复文件。重置进度按钮不等于删除已导出文件、所有恢复副本或源服务器数据；私有恢复文件是否进入系统备份取决于系统备份设置。
- 隐藏封面仅隐藏书库封面并暂停加载，不清除缓存，不锁定阅读，也不保证系统多任务预览隐藏全部内容。

## 控制方式

设置中可以清理封面缓存、重置阅读进度、解除当前服务配对。解除配对删除客户端保存的凭据，并不自动撤销服务端 token；泄露时还应在服务端撤销或更换凭据。用户需自行管理已导出的 JSON 及系统备份。

## Apple 隐私清单

`ios/PrivacyInfo.xcprivacy` 声明无开发者数据收集、无跟踪，并列出当前必需理由 API：

| 类别 | 理由 | 实际用途 |
|---|---|---|
| UserDefaults | CA92.1 | 应用自身偏好与阅读位置 |
| FileTimestamp | C617.1 | 应用容器内封面缓存的过期与淘汰 |
| SystemBootTime | 35F9.1 | 请求节流、应用内事件间隔与计时 |

参考 [Apple 必需理由 API 说明](https://developer.apple.com/documentation/bundleresources/describing-use-of-required-reason-api)。该声明依据当前代码填写，不是 App Store 审核证明。若加入遥测、远程服务、SDK 或新接口用途，必须重新核对隐私清单和对外说明。
