# 安全范围与报告

这是面向可信私人局域网的开发预览客户端和安卓桥接，不是公网文件服务器或加密阅读工具。当前版本未接受独立渗透测试，无法承诺没有漏洞。

## 已有防护与限制

- 地址检查限制私人 IPv4，拒绝 URL 用户信息、意外路径与跨地址重定向。私人地址限制不等于网络中没有攻击者。
- 六位配对码只用于换取服务端阅读 token；有效期、尝试次数和随机质量依赖兼容服务端实现。
- 已配对服务通过 HMAC 身份证明后才发送阅读 token，但首次配对和后续数据均仍走 HTTP。主动中间人、旁路监听或服务端失陷仍在风险范围内。
- 限制响应体大小、列表字段、页码与缓存容量，并取消过时任务。这不是恶意图片解码攻击的完整沙箱保证；请及时更新 iOS。
- 不提供公网模式、TLS 配置、账户权限系统或远程文件写入；进度只在本机保存／手动导出。

不要把阅读服务开放公网，不要把端口转发／公开隧道当作受支持功能。不要在公共或访客网络上配对。凭据泄露时先在服务端撤销或轮换，再重新配对。

安卓六位码有时效和尝试上限，但备用二维码直接携带长期 token，不受六位码过期限制。请勿公开二维码；泄露后用安卓“重置配对”撤销。桥接以只读授权读取所选目录；不对客户端做逐书访问控制，持有 token 的客户端可读取已分享书库。

安卓临时导入副本可能含上游备份的其他敏感字段，即使程序只查询下载表；正常流程尝试删除，进程被强杀后可能遗留在应用缓存中。若需共享日志、截图或缓存目录，先人工检查并脱敏。第三方库／系统日志不在“应用自身不记录访问历史”的保证范围内。

## 报告问题

发布者应在 GitHub 仓库设置中开启 Private vulnerability reporting。开启后，请使用仓库 Security 页面中的私密报告入口。若入口尚未开启，不要把可利用漏洞细节或秘密发到公开 Issue；可以只提出“请开启私密安全报告”的无敏感信息请求。

报告只需最小合成复现、系统及应用版本和影响说明。不要提交真实书库、书名、地址、token、PIN、个人邮箱、证书或原始设备日志。本仓库不声明响应 SLA。
