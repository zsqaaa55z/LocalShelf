# 贡献指南

- 提交前运行 `sh scripts/check.sh`，并为行为变化补充合成测试。
- 只提交你有权许可的内容；有第三方代码时列明来源、版本与许可证，不用“参考”掩盖复制或改编。
- 默认提交的自有贡献按本项目 MIT 提供。系统 SDK、第三方资源的许可不因此改变。
- 不提交漫画内容、个人头像、用户阅读记录、设备日志、配对码、证书或 `.env`。
- 真机签名使用自己的 Team 和 Bundle Identifier；不要把个人签名配置带回提交。
- 交互改动应保留下载列表顺序、缺失占位、页码顺序及取消操作的一致性。
- 性能报告区分模拟测试和真实设备测量，不把计时回调等同实际屏幕帧率。
- 若希望保护身份，提交前自行设置公开昵称与 GitHub 提供的 noreply 邮箱；不要使用真实邮箱创建提交后再期望 `.gitignore` 隐藏它。
